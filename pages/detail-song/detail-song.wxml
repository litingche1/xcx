<!--pages/detail-song/detail-song.wxml-->
<view class="title">
  {{songInfo.name}}
</view>
<view class="song-list"> 
  <block wx:for="{{songInfo.tracks}}" wx:key="id">
  <sont-item-v2 itemData="{{item}}" index="{{index+1}}"></sont-item-v2>
  </block>
</view>
